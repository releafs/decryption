name: Image Processing Pipeline

on:
  push:
    branches:
      - main
    paths:
      - 'decryption/input/**'

jobs:
  process-images:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Allow pushing changes to the repository

    steps:
    # Step 1: Check out the repository
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        persist-credentials: true

    # Step 2: Cache pip dependencies
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    # Step 3: Set up Python environment
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    # Step 4: Install dependencies
    - name: Install Dependencies
      run: |
        pip install -r requirements.txt

    # Step 5: Ensure the input directory exists before running any scripts
    - name: Create input directory if missing
      run: mkdir -p decryption/input

    # Step 6: Debugging - List files in input directory
    - name: Check input directory for PNGs
      run: |
        echo "Checking input directory for PNGs:"
        ls -la decryption/input/

    # Step 7: Run the main processing scripts
    - name: Run processing scripts
      run: |
        python scripts/script1.py
        python scripts/script2.py
        python scripts/script3.py

    # Step 8: Verify if the output file exists in the output directory
    - name: Verify output file exists
      run: |
        if [ ! -f "output/merged_data_with_metadata.csv" ]; then
          echo "Output file not found!"
          exit 1
        else
          echo "Output file found!"
        fi

    # Step 9: Stash local changes, pull from remote, apply stash, and commit processed results
    - name: Stash, pull, resolve conflicts, and push processed result
      run: |
        git config --global user.email "action@github.com"
        git config --global user.name "GitHub Action"
        git stash
        git pull origin main --rebase || true
        git stash pop || true
        git add output/merged_data_with_metadata.csv
        git diff --cached --quiet || git commit -m "Add processed result for ${{ github.sha }}"
        git push

    # Step 10: Upload the artifact from the output directory
    - name: Upload processed result as artifact
      uses: actions/upload-artifact@v4
      with:
        name: processed-results-${{ github.sha }}
        path: output/merged_data_with_metadata.csv

    # Step 11: Clean up output directory (Optional if needed)
    - name: Clean up output directory
      run: rm -rf output/*

    # Step 12: Run script4.py to delete PNG files and input directory after processing
    - name: Delete input files and directory after processing
      run: python scripts/script4.py
